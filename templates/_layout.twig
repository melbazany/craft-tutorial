<!DOCTYPE html>
<html lang="{{ craft.app.language }}">
  <head>
    <meta charset="utf-8"/>
    <title>{{ siteName }}</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">

    {% do craft.app.view.registerCssFile('@web/styles.css') %}
  </head>
  <body>

  {% include "_layout-components/_header" %}

    <main>
      {% block content %}
        {# Nothing here, yet! #}
      {% endblock %}
    </main>

  {% include "_layout-components/_footer" %}

  </body>
</html>
